import React from'react';import{StyleSheet,Text,View}from'react-native';import{AppCard,Chip,ScreenContainer,ScreenTitle,SectionHeader,TopicCard}from'../../components';import{subjects}from'../../data/subjects';import{Difficulty,StoredAppData,SubjectId}from'../../types';import{colors,spacing,typography}from'../../theme/tokens';type Props={data:StoredAppData;difficulty:Difficulty;onDifficultyChange:(difficulty:Difficulty)=>void;onSelectSubject:(subject:SubjectId)=>void};export function PracticeScreen({data,difficulty,onDifficultyChange,onSelectSubject}:Props){const format=(value:Difficulty)=>`${value.charAt(0).toUpperCase()}${value.slice(1)}`;return <ScreenContainer><ScreenTitle title="Practice & Custom Drill" subtitle="Choose a difficulty, then a single skill or Mixed for a custom all-round drill."/><AppCard style={styles.setupCard}><Text style={styles.setupLabel}>Difficulty</Text><View style={styles.chips}>{(['beginner','intermediate','advanced']as Difficulty[]).map(item=><Chip key={item} label={format(item)} selected={difficulty===item} onPress={()=>onDifficultyChange(item)}/>)}</View></AppCard><SectionHeader title="All Skills"/><View style={styles.topicGrid}>{subjects.map(subject=><TopicCard key={subject.id} title={subject.name} iconText={subject.iconText} subtitle={subject.id==='mixed'?`${format(difficulty)} custom mix`:`Level ${data.progress.topicStats[subject.id]?.level??1}`} accent={subject.accent} softAccent={subject.softAccent} onPress={()=>onSelectSubject(subject.id)}/>)}</View></ScreenContainer>}const styles=StyleSheet.create({setupCard:{gap:spacing.md},setupLabel:{color:colors.ink900,fontSize:typography.cardTitle,fontWeight:'900'},chips:{flexDirection:'row',gap:spacing.sm},topicGrid:{flexDirection:'row',flexWrap:'wrap',gap:spacing.md}});