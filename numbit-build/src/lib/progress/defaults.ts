import {subjectIds} from '../../data/subjects'; import {AppSettings,DailyMission,StoredAppData,SubjectId,TopicStats,UserProgress} from '../../types'; import {getLocalDate} from '../date/localDate'; export function createDefaultTopicStats(subject:SubjectId):TopicStats{return{subject,attempts:0,correct:0,bestScore:0,xp:0,level:1,recentResults:[],recentAnswerTimesMs:[]}} export function createDefaultProgress():UserProgress{return{totalXp:0,level:1,currentStreak:0,bestStreak:0,totalAttempts:0,totalCorrect:0,totalAnswerTimeMs:0,fastestCorrectMs:0,bestMathRating:0,bestScore:0,roundsCompleted:0,quickSprintsCompleted:0,mistakesImproved:0,topicStats:Object.fromEntries(subjectIds.map(subject=>[subject,createDefaultTopicStats(subject)])) as Record<SubjectId,TopicStats>,weeklyActivity:[0,0,0,0,0,0,0]}} export function createDefaultSettings():AppSettings{return{soundEnabled:true,hapticsEnabled:true,preferredDifficulty:'beginner',onboardingComplete:false}} export function createDefaultDailyMission(subject:SubjectId='addition',localDate=getLocalDate()):DailyMission{return{id:`daily-${localDate}-${subject}`,localDate,subject,difficulty:'beginner',title:`Score 80% or higher in ${subject==='timesTables'?'Times Tables':subject}`,targetAccuracy:80,questionCount:10,xpReward:50,completed:false,passed:false,progress:0}} export function createDefaultStoredData():StoredAppData{return{progress:createDefaultProgress(),mistakes:[],roundHistory:[],settings:createDefaultSettings(),dailyMission:createDefaultDailyMission()}}